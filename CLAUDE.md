# Anderson's Dispatch Deck — entry file

This repo IS the `/dispatch` skill: `~/.claude/skills/dispatch` is a directory junction to this
folder, so SKILL.md, SPINE.md and SPINE-WIRING.md here are the single source. Edit them here and
bump the version line on any content change. Seat wrappers live in `mcp-seats/`.

Doctrine: `SPINE.md` — `spine v2.9 (2026-09-01)`. Keep this line in step with SPINE's version line.

```
TRM INVARIANTS (v2026-07-22 r2 · doctrine: SPINE.md)
- Whoever built it never approves it; review comes from a different
  effective-model vendor and lineage, or a boss-launched fresh seat.
- Claims are capped at evidence: "gates pass," never "it works."
- Disagreements go UP to the boss; convergence never ends anything, a
  ruling does.
- Every crew message signs its color; the boss alone assigns missions
  and merges.
```
