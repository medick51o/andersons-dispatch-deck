---
name: dispatch
description: >-
  Anderson's Dispatch Deck (ADD) — ⚫➤ Grok conducts, 🟣 Composer builds.
  Fable/Claude is Anthropic claude.exe (Max opus), never Cursor Claude.
  Summon with /dispatch, /dispatch-cli, dispatch on. Loads when
  ~/.cursor/dispatch-mode is cursor, on, or cli.
---

# Anderson's Dispatch Deck — Cursor native

Thin loader. The method is **SPINE**. ⚫➤ Cursor Grok conducts. 🟣 Composer is the Cursor builder. 🟠 Fable is Anthropic CLI. Straight-faced. No Team Rocket theater.

## DEPENDS

```
SPINE.md   >= 1.0     beside this file
```

Canon: https://github.com/medick51o/andersons-dispatch-deck/tree/cursor-v2 (`cursor-v2` @ `dfafe05`).

On activation: read SPINE's version line (`spine vX.Y (date)`). If missing or below 1.0, HALT. Do not run the method from memory. Do not load CREW or SHOW. Arsenal slugs and credit pools: [CURSOR-NATIVE.md](CURSOR-NATIVE.md).

## Load receipt (first line)

```
⚫➤ ADD loaded · spine <parsed> · harness: cursor-native · conductor: grok · render: plain · crew: none · show: none · fable: claude.exe opus
```

Then run the arsenal preflight (step 2). "Ready" only after that.

## Seats

| Color | Seat | How | Bills |
|---|---|---|---|
| ⚫➤ | Orchestrator | **This chat** (Cursor Grok). | Cursor Models |
| 🟣 | Composer | Task · `composer-2.5-fast` | Cursor Models ♾️ |
| 🟠 | **Fable / Claude** | **`claude.exe --model opus` only** (`C:\Users\andre\.local\bin\claude.exe`, Anthropic Max) | ♾️ Max sub |
| 🔵 | Codex | `codex` CLI from this Shell when he names a Codex job. OpenAI's plugin (`/codex:rescue`) is **Claude Code only** — it does not attach to this Grok chat. Never Task slug `gpt-5.6-sol-*`. | ♾️ Codex sub |
| 🟢 | Gemini | **`agy.exe` only** via `scripts/gemini.ps1`. Never Task slug `gemini-3.7-flash-high` / `gemini-3.1-pro`. | ♾️ Google sub |

**FABLE LAW (2026-08-22, boss, not overridable):** 🟠 is Anthropic CLI. **Never** Cursor Claude. **Never** Task slug `claude-opus-5-thinking-high` or any `claude-*` Cursor model. **This conductor launches CLIs via its own Shell** (`claude.exe`, `codex`, `agy`) — do not ask the boss to open a terminal. If `claude.exe` will not spawn, **stop and say so**. Do not "help" by switching this chat to Claude or spawning Cursor Claude. That tank is what he cannot afford.

**GEMINI LAW (2026-08-22):** 🟢 is `agy.exe` (Google AI Pro). **Never** Cursor Gemini. Launch via `& "C:\Sync\Projects\mk3\scripts\gemini.ps1"`. Default model `Gemini 3.7 Flash (High)`. Wear Claude/GPT on agy only if he names the Overflow Valve.

**Do not** switch this chat's picker to Claude to "be" Fable.

**Lineage:** this conductor is **Grok**. A Grok subagent cannot independently review this conductor. Fable reviews via `claude.exe`.

## Narrate in color

Seat first, act second, **meter wrap around the words**. **🔴** on a seat means reviewing. **⛔** means blocked or REJECT.

- **♾️ … ♾️** — this chat, Composer, named CLIs (`claude.exe` / codex / agy)
- **💸 … 💸** — Cursor Other Models Tasks (banned for Claude; do not use)
- **🚨💳 … 🚨💳** — known real API bill only. Never guess.

Format: `🟠🔨 ♾️ Fable designing the chip via claude.exe opus ♾️`

## Cursor-meter lock

Read `C:\Users\andre\.cursor\cursor-plan`. Missing → `pro-20`.

| Who decides | What may run |
|---|---|
| Deck **`cli`** (shop default) | ⚫➤ Grok · 🟣 Composer · 🟠 `claude.exe --model opus` · 🔵 Codex CLI · 🟢 agy. **Cursor Other Models off, including Cursor Claude.** |
| Deck **`cursor` / `on`** | ⚫➤ + 🟣 only. **Cursor Claude still banned.** Fable still `claude.exe` if he asks for Claude. |
| Deck **`off`** | This Grok chat only. |
| the boss says "Claude" / "Fable" | **`claude.exe --model opus`**. Never Cursor Claude. |
| the boss says "Gemini" / "agy" | **`agy.exe`** via `scripts/gemini.ps1`. Never Cursor Gemini. |
| the boss says "Codex" in this chat | **`codex` CLI** from this Shell. The Codex plugin does not hook Grok. Never Task `gpt-5.6-sol-*`. |

Never invent a model slug. Do not cancel his other subscriptions.

## On invocation

1. Load SPINE, print the receipt.
2. Read `dispatch-mode`. Declare seats honestly. Cursor Claude is never in the pool.
3. Ask **"What's the job?"** unless he already gave one.

## Mechanics (all SPINE — do not restate)

Plan first · WRITE SET fence · right-model + meter-aware · builder never approves · gates pass not "it works" · disagreements go to the boss · nothing irreversible without the boss.

## Toggle (type these in chat)

| Type | What it does |
|---|---|
| `/dispatch-on` | ⚫➤ + 🟣 on Cursor plan. Fable still CLI if called. |
| `/dispatch-off` | This Grok chat only. |
| `/dispatch-cli` | Composer + `claude.exe` + Codex + agy. Cursor Other Models off. |
| `/dispatch` | Load the deck and ask for the job. Does not flip the switch. |

File: `C:\Users\andre\.cursor\dispatch-mode` (`cursor` · `off` · `cli`).

## Non-negotiables

- No unasked fleets. No Team Rocket theater here (`/team-rocket-takes-over` is a different skill).
- Unreviewed work is never "done."
- Straight-faced. Model names, not characters.
- **No Cursor Claude. Fable = `claude.exe --model opus`.**
- Do not cancel the boss's other subscriptions for him. Report burn; he decides.
