SIGNED: Cursor Composer 2.5 · 🎼 · Composer

**Gold:** **Grok in this chat** conducts Cursor day-to-day. I am the **bounded builder**, not the baton. Gate-0 small work stays Grok; anything fenced gets Grok plan → dispatch → gate → report. I should **never be default gold** — I lose parent-chat context, I'm optimized for execution not meter-aware orchestration, and putting me in gold burns the Cursor Models pool on narration. Exception: boss explicitly says vanilla / Grok unreachable / single-agent — then degraded solo, signed 🎼, not "ADD cruise."

**Seat table**

| Seat | Own | Never |
|---|---|---|
| **🟡 Grok (conductor)** | Plan, fence, Gate-0, dispatch ledger, UI judgment, spawn Task workers, declare independence status | Build whole slices alone when fence warrants 🎼; fake cross-vendor review via Grok worker; burn Other Models unprompted on Pro |
| **🎼 Composer 2.5** | Disjoint WRITE SET builds, mechanical refactors, test/lint fixes, explore/shell investigation **inside a ticket**, parallel lane when Grok fences | Gold; architecture on substantial work; councils; claiming 🔵-grade independent review of Grok output |
| **🔵 GPT-5.6 Sol** | Code review when it **didn't build** (Ultra / boss-named on Plus) | Default on Pro $20; conductor |
| **🟠 Claude Opus** | Architecture, judgment calls, heavy review (Ultra / named) | Pro default; one Opus "test" eats the $20 tank |
| **🟢 Gemini 3.7 Flash High** | Cheapest **named** Other Models reviewer + overflow tie-break | Auto-spawn on Pro; gold |
| **🟢 Gemini 3.1 Pro** | Heavier doc/arch pass when Flash thin (named / Ultra) | Cheap-seat substitute for Claude on hard judgment |
| **⚫ Grok worker** | Parallel Grok lane, same-vendor overflow build | Independent review of this conductor's Grok |
| **⚫ grok.exe CLI** | Boss-named overflow when IDE pool saturated | Default harness; silent parallel to Cursor Grok |
| **Claude/Codex CLIs** | Boss-named `cli` harness overflow | Cursor-native default |

**Gemini 3.7 one sentence:** On boss go (Pro Plus+) or Ultra cruise, dispatch **`gemini-3.7-flash-high`** as a **read-only, fenced-file 🟢📝 review Task** after 🎼 or ⚫ builds — cheapest real cross-vendor vote, logged to Other Models, never auto on `pro-20`.

**Pro $20 review honesty:** Status is **`SOLO-VENDOR DEGRADED`** — full stop. Composer 📝 on Grok 🔨 is a **verification pass** (tests, types, obvious bugs), **not** SPINE independent review: same Cursor shop, correlated blind spots, I often lack full conductor context. Legal done-rung on Pro: **gates pass + boss in-hand**, or **boss names one Other Models seat**. Never print `FULL CROSS-VENDOR`. I *can* review Grok honestly as a second pair of hands; I *cannot* honestly certify cross-vendor independence.

**Cursor Task vs CLI — one SKILL.md:** `SKILL.md` = thin loader + **`dispatch-mode` router** (`cursor` | `cli` | `off`); shared **`SPINE.md`**; **`CURSOR-NATIVE.md`** = Task slugs, pools, plan ladder, ledger; **`CLI-OVERFLOW.md`** = trunk wiring (`claude.exe`, `agy`, Codex plugin). Preflight reads `cursor-plan` + mode **before** any spawn — one doc, two harnesses, no wrong-pool oops.

**Delete vs keep:** **Delete** Fable-default gold, Gemini-spare-only, Grok-UI-only — wrong for 2026-08 Cursor. **Keep** all SPINE mechanics: WRITE SET fence, builder-never-approves, gates-not-"it works", color timeline, load receipt, anti-picker-laundering, meter-aware routing, straight-faced render. **Add** 🎼 to legend; rename solo same-pool pass to **verification** vs **independent review**.

**Files (`cursor-v2` branch):** `skills/dispatch/SKILL.md`, `SPINE.md`, `CURSOR-NATIVE.md`, `CLI-OVERFLOW.md`, optional `PLAN-LADDER.md`; repo stub `dispatch-mode.example` + pointer in mk3 `MACHINE.md`. No duplicated SPINE forks.

**Disagreements I expect:** Grok arguing co-gold 🎼 for speed; Claude insisting zero Composer review; Gemini pushing Flash as default even on Pro; whether Cursor Models = one vendor for SPINE lineage (I say **degraded same-shop** until 🔵/🟠/🟢 Task fires).

**Confidence:** **High** on pool split, existing cursor-native draft, and Pro frugality rules — read `CURSOR-NATIVE.md`, mk3 `CURRENT-WORK.md`, cursor `SKILL.md`. **Not verified:** live Task slug list, dashboard rates, SPINE Part IV exact lineage text, Pro Plus `$70` Other Models figure beyond the draft table.
