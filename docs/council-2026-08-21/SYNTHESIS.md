# COUNCIL SYNTHESIS — cursor-v2 ADD
*Convener: Cursor Grok 🟡 · 2026-08-21 · five signed reads · boss rules, council advises*

**Independence:** 🟠 Claude Max CLI · 🔵 Codex CLI · 🟢 Gemini 3.7 Flash (agy) · 🎼 Composer 2.5 (Cursor Models) are different producing lines. ⚫ Super Grok CLI is **same vendor as this conductor** — cost specialist, not a second vote. Discount any place I “agree” with Grok CLI.

## Consensus (all independent seats, unless noted)

1. **Gold in Cursor = Grok.** Not Claude (bill). Not Composer (execution, not orchestration). Not picker-switching as the default.
2. **Composer 2.5 = default in-pool builder.** Never gold. Never “independent review” of Grok under SPINE as written.
3. **Gemini 3.7 Flash is first-class.** Delete “cheap extra / 4th vote / spare tire.”
4. **Pro $20 cannot print `FULL CROSS-VENDOR` from Cursor Tasks.** Grok↔Composer is a verification pass / harness-degraded, not Codex. Honest labels or CLI review.
5. **Keep SPINE:** fences, builder-never-approves, council ask-first, gates ≠ “it works.” **Delete:** Fable-as-gold, Grok-UI-only, Gemini-as-spare.

## Named disagreements (do not smooth)

| Topic | Who | Split |
|---|---|---|
| Where 3.7 Flash *lives* | 🟠 Claude + ⚫ Grok CLI + 🟢 Gemini | First-class on **`agy` CLI** (Google tab) so Pro $20 still gets a real reviewer |
| | 🎼 Composer | First-class as a **named Cursor Task** on Plus/Ultra; **never auto on Pro** |
| | 🔵 Codex | Capability is first-class; **billing posture is a separate fact** (Cursor Task = Other Models, parked; agy = overflow) |
| Default reviewer on Pro | 🟠 Claude | Flash High on agy is the **standing first reader, every gate** |
| | 🎼 Composer | Pro stays **SOLO-VENDOR DEGRADED** until the boss **names** a seat |
| Claude’s job | 🟠 Claude (self) | Kill standing “architect **and** judge.” Spec author is disqualified from judging that spec. Keep 1M spec + gate discipline as **escalation**, not a title |
| | 🔵 Codex | Keep a parked `claude-architect` seat for judgment / gnarly review |
| Enforce the bill fence | 🔵 Codex | `subagentStart` hook + `.cursor/agents/*.md` — prose cannot stop a spawn |
| | others | SKILL.md + `METER:` / `[HARNESS:]` tags |
| SPINE on the branch | 🟠 Claude | **Copy**, not pointer (trunk move must not break Cursor) |
| | 🔵 Codex | **One canonical SPINE**, don’t duplicate |
| Grok.exe | ⚫ Grok CLI | Park it. Second Grok bill almost never worth it; cannot review Cursor Grok |
| Composer reviewing Grok | 🎼 Composer | Allowed as **verification**, not independence |
| | 🔵 Codex | If that becomes an *approving* path, **amend SPINE** — don’t euphemize on a branch |

## Best-of-breed draft (for you to accept or cut)

**Daily (Pro $20, `dispatch-mode: cursor`):** 🟡 Grok conducts · 🎼 Composer builds · reviews = **verification** or a **named CLI** (`agy` 3.7 / `codex` / `claude.exe`). Other Models Tasks stay parked.

**Gemini 3.7 Flash:** first-class **CLI workhorse** (build + first honest read of Cursor output) on Google’s tab. Cursor Gemini Task stays parked until you name it or `cursor-plan` is `pro-plus`/`ultra`.

**Claude Max CLI:** escalation spec / hard judgment / review of work it did **not** specify. Not gold. Not daily judge-of-record.

**Codex CLI:** sharpest code review + fenced impl. Not direction.

**Grok.exe:** overflow UI/build only; never a reviewer of this chat.

## Rulings needed (react)

1. Gemini 3.7 default home: **agy-first** vs **Cursor-Task-when-named** vs **both, bill-fenced**?
2. On Pro, is a slice **done** after Composer verification + your in-hand, or must a CLI reviewer fire?
3. Claude: **escalation-only** (his refute) vs parked architect+judge title (Codex table)?
4. SPINE on `cursor-v2`: **copy** or **pointer**?
5. Install Codex’s **hook guard** this branch, or skill-prose only for v1?

Sources: `docs/council-2026-08-21/SIGNED-{claude,codex,gemini,grok,composer}.md`
