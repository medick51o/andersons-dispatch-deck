VERDICT: The monolith is the wrong shape — it conflates "always binding" with "always loaded," and only the canonical invariant block is genuinely load-bearing at every moment. Split into a small always-loaded core (the invariant block + the four structures + triggered pointers) and on-demand modules; the appendix split is correct but the color map must stay in core. The real risk is pointer-trigger failure, not information loss — name that and the restructure is safe; ignore it and you've built a cheaper monolith that silently drops law.

Q1 THE APPENDIX SPLIT — yes, with one carve-out. Failure mode: Appendix A's color→vendor map is referenced by Part VIII's signature mechanic and by every tier's launcher at render time. Move it out and leave it unloaded, and signatures become illegible — the orchestrator can no longer tell which seat signed what, and Principle 1's "distinct, visible identities" silently breaks. Keep the color map in core (~4 lines); move the rest (CLI commands, paths, agy strings, ledger location) on demand.

Q2 WHERE THE WEIGHT IS

[CUT 1] Thin/missing support + ENTRY-primary prose (~450 tokens)
ANCHOR: "When the support seat is thin or missing. The adversarial channel does not require a rich second vendor"
WHY SAFE: The posture table (lines 404–409) plus the anti-laundering guard's two legal paths (Part IV) already state the legal review paths; these sections are scenario walkthroughs of those same rules.
ACTION: COMPRESS to a 3-line pointer: "Thin/missing support → the two legal review paths (Part IV) still bind; ENTRY primary → small slices, fan-outs off, second vendor's entry tier reviews. Pretending a mid seat is frontier kills the bar."

[CUT 2] Routing-ledger paragraph (~180 tokens)
ANCHOR: "The routing ledger — every dispatch writes one line, the mission report prints them, with `default` and `changed?` columns"
WHY SAFE: The load-bearing rule is one line per dispatch with default/changed? columns; the surrounding 11 lines are honesty caveats that restate the Ladder's claims-capped principle.
ACTION: COMPRESS to the rule + one caveat line.

[CUT 3] Declared-seat-lineage + anti-laundering + continuity (~300 tokens)
ANCHOR: "The declared-seat-lineage clause. Orchestration means the orchestrator technically launches the workers"
WHY SAFE: Principle 3 plus the effective-model preflight (Part VI) already state the fixed point; these clauses exist only to defend against a literal reading that would ban all internal review.
ACTION: COMPRESS to one paragraph: "A charter-declared seat is its own lineage even when launched by another; a producer-launched same-vendor context wearing a crew name is a spawn, not a reviewer; succession inherits restrictions."

[CUT 4] Disputed-finding escalation (~200 tokens)
ANCHOR: "A disputed finding escalates on the strongest falsifiable evidence available, and "no test exists" NEVER means NOT PROVEN."
WHY SAFE: The rule is the first sentence plus "untestability is never evidence against a finding"; the rest elaborates test-authorship mechanics already covered by Principle 3.
ACTION: COMPRESS to two sentences.

[CUT 5] METER LAW clauses 5 & 7 (~150 tokens)
ANCHOR: "Cost claims cite a reading, not a recollection. "That's cheap" is "it works" wearing a hat."
WHY SAFE: Clause 5 restates the Ladder's claims-capped; clause 7 ("The vendor draws the needle") restates clause 2's measure-never-infer.
ACTION: COMPRESS — fold clause 5 into clause 2, clause 7 into a one-line caveat.

[CUT 6] Autonomous-hours token discipline (~250 tokens)
ANCHOR: "AUTONOMOUS-HOURS TOKEN DISCIPLINE (the anti-token-inferno core; CREW carries the crew-flavored telling)."
WHY SAFE: Principle 8's loop cap already binds; this is the unattended elaboration. The load-bearing bits are four: two rounds per debate then bell; blocked → pivot not idle; batch taste questions; in doubt → safest honest version + note loudly.
ACTION: COMPRESS to those four bullets, drop the prose.

Q3 STRUCTURE — restructure. Replace the monolith with a small always-loaded CORE plus on-demand MODULES, loaded by triggered pointers in the core.

CORE (always-loaded, ~1.8k tokens):
- The canonical invariant block (Part VIII, lines 709–718) — the byte-identical compliance floor; this already exists and is the only text that must bind behavior even if nothing else loads.
- The four load-bearing structures (Part I, ~1.3k tokens) — the engine's actual decision points; an orchestrator must have them before any dispatch.
- The color→vendor map (carved from Appendix A, ~4 lines) — render-time load-bearing.
- One triggered pointer per module: "IF about to dispatch → load MECHANICS; IF about to fan out → load FLEET; IF running a review → load ADJUDICATION + REVIEW-CULTURE; IF spending → load METER + COUNCIL-SEAT; IF wiring → load TRANSPORT + APPENDICES."

MODULES (on-demand): DOCTRINES (Part II), PRINCIPLES (Part III), FLEET (Part IV), ADJUDICATION (Part V), MECHANICS (Part VI — the largest, ~3.5k), REVIEW-CULTURE (Part VII), METER + COUNCIL-SEAT + TRANSPORT (the three standalone laws), APPENDICES A & B.

What it sacrifices: the property that every orchestrator has read every law. Under the monolith, you can assume the orchestrator has seen Part IV's fleet test even on a task that never fans out. Under modules, an orchestrator that never contemplates parallelism never reads the fleet test — and then one day it does, and doesn't know the test exists. The mitigation is the triggered pointer, and that is the load-bearing assumption: the pointer must fire reflexively before the act, not after. If the trigger fails, the restructure has silently dropped law — which is exactly the owner's standing prohibition. So the restructure is safe IF AND ONLY IF the core's pointers are written as gates ("do not dispatch until MECHANICS is loaded"), not as suggestions.

DEFENDED — what must NOT move:
- The canonical invariant block (Part VIII). It is already the byte-identical floor copied into every entry file and launcher; moving it to a module defeats its entire purpose.
- The four load-bearing structures (Part I). These are the engine's actual decision points; an orchestrator without them cannot classify a task or judge evidence.
- The Ladder of Truth's top rung (boss in-hand). The fixed point that survives every seat flip; if it's not loaded, nothing stops a model from declaring victory.
- The color→vendor map. Render-time load-bearing per Q1.
- Principle 3 (builder never reviewer) — though it can live as a pointer because the invariant block already carries it verbatim ("Whoever built it never approves it").

TOTAL: ~3,300 tokens always-recoverable per summon (appendices + the six compressions). Structurally, on a small task needing only CORE + DOCTRINES + MECHANICS, recover up to ~10,000 tokens; on a task needing every module, ~3,300. The headline number that holds in the worst case is ~3,300.

CONFIDENCE: medium-high on the appendix split and the six compressions — they are clearly elaboration of rules stated elsewhere, and the anchors survive. Medium on the full restructure — the compliance-floor risk is real and the pointer-trigger mechanism is the load-bearing assumption I cannot verify without a test. What would change my mind: a test run where orchestrators given only the core are observed to dispatch without pulling MECHANICS, or to fan out without pulling FLEET. If that failure fires even once, the monolith is justified and only the compressions stand. If a test shows the triggers fire reliably, the restructure is the bigger lever and the monolith should go.