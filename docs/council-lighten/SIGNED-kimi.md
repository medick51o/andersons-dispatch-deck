VERDICT (3 sentences max)

The appendix split is right for B and for most of A, but A contains one piece of per-line output law (the notation/meter-mark grammar) that is memorize-not-lookup, and splitting it wholesale recreates the exact failure the monolith exists to prevent. The engine's real weight is not dead law but a small set of conditional subsystems — council procedure, autonomous-hours discipline, posture case-analysis, episode/visuals mechanics — each with a natural, observable load-trigger, plus a second layer of repetition the first pass missed (the consent gate is stated five times; the two legal review paths four times). A core-plus-triggered-modules structure is safe here only because the triggers are actions, not judgment calls — but the consent gates and the containment rule must never leave the trunk.

Q1 THE APPENDIX SPLIT — yes, with a carve-out. Move Appendix B and the vendor-CLI/path/bloodline wiring. Do NOT move the notation block wholesale: "METER MARKS ARE MANDATORY ON ANY LINE THAT CAN SPEND" is a rule the orchestrator must *have memorized* — it applies to every dispatch line it emits, and you cannot look up a grammar per-line. The failure mode: silent non-compliance — the orchestrator emits unsigned, unmetered lines and nothing in the trunk tells it a notation exists, because Part VIII only pointers the *color map* ("the vendor→color map (Appendix A)"), not the marks. An orchestrator that must know-it-can-check the agy `--model` syntax is fine on-demand; an orchestrator that must produce `🟣➤🌙 💸` on every spending line is not. Move the color map + notation + meter-mark grammar into the loader (SKILL.md) or a small trunk section; split the rest. Net saving slightly under the estimated ~2,116 — call it ~1,700.

Q2 WHERE THE WEIGHT IS

[CUT 1] Duplicate ship-rule (~50 tokens)
ANCHOR: Green gates + passed bench + working in-hand = shipped
WHY SAFE: Stated in full at Part I §1 ("The bench catches CODE bugs; the boss catches REALITY bugs"); Doctrine 1 gate 4 re-states it verbatim.
ACTION: COMPRESS Doctrine 1 gate 4 to "BOSS IN-HAND — the TOP gate (Ladder of Truth, Part I §1 owns the shipped rule)."

[CUT 2] Consent gate stated five times (~80 tokens)
ANCHOR: dispatches only on the boss's explicit go, never self-authorized
WHY SAFE: Part I §2 owns it; it is re-stated near-verbatim in Doctrine 1 gate 1, Doctrine 5, Part VI's dispatch gate, and THE COUNCIL. The COUNCIL instance is the load-bearing one for the special move; the other three become pointers.
ACTION: COMPRESS three instances to "consent-gated per Part I §2."

[CUT 3] Anti-laundering restated four times (~60 tokens)
ANCHOR: a different account merely hosting the builder's own brain does NOT count
WHY SAFE: Part IV's anti-laundering guard owns it; it reappears in Part III's abstract roles, Part VI's Review dispatch, and TRANSPORT LAW clause 2. Part IV's version is the most complete.
ACTION: COMPRESS Part VI and TRANSPORT LAW to "Part IV's two legal paths bind"; keep Part III's since roles are definitional.

[CUT 4] stdin trick duplicated (~30 tokens)
ANCHOR: when the reviewer can't read the repo, HAND IT THE CODE via stdin
WHY SAFE: Review dispatch already carries "hand the reviewer the code itself via stdin."
ACTION: DELETE from the three-flips scars paragraph.

[MOVE 5] Posture case-analysis (~600 tokens)
ANCHOR: When the support seat is thin or missing.
WHY SAFE: The posture TABLE and five levers stay in trunk (routing happens every task); the support=ENTRY/MINIMAL/NONE and ENTRY-primary paragraphs are consulted only when a shop's card is thin — a declared, observable card state. Survives as `SPINE-POSTURES.md`, trigger: "card declares ENTRY/MINIMAL/NONE support → MUST read before routing."
ACTION: MOVE to on-demand.

[MOVE 6] Council procedure (~450 tokens)
ANCHOR: The procedure the orchestrator runs — a defined path, not an improvisation
WHY SAFE: The council is consent-gated by definition — the boss's explicit go IS the load trigger, and it can never fire without one. Trunk keeps: when-to-convene, when-NOT-to-convene, the consent gate, and "on go, MUST run the procedure in SPINE-COUNCIL.md."
ACTION: MOVE steps 1–6 to on-demand.

[MOVE 7] Autonomous-hours discipline (~350 tokens)
ANCHOR: AUTONOMOUS-HOURS TOKEN DISCIPLINE (the anti-token-inferno core; CREW carries the crew-flavored
WHY SAFE: It binds only "when the shop runs unattended" — the boss declaring autonomous hours is an observable trigger. Trunk keeps one line: "Unattended run declared → MUST load SPINE-AUTONOMOUS.md before proceeding; the bell and the decision queue bind absolutely."
ACTION: MOVE to on-demand.

[MOVE 8] Episode visuals + credits style law (~450 tokens)
ANCHOR: RE-COMPRESSED to economical JPEG (cap ~1280px on the long edge, quality ~70
WHY SAFE: Needed only when a screenshot arrives or an episode closes — both observable events. The ending-credits exception is SHOW-tier presentation law sitting in a character-free engine; it belongs to SHOW's tier docs or the same on-demand file.
ACTION: MOVE to on-demand (episode-mechanics file).

[MOVE 9] Invariant-block revision history (~120 tokens)
ANCHOR: bumped r1 → r2 on 2026-07-22, when "another vendor's account" was tightened
WHY SAFE: Pure provenance; SPINE-PROVENANCE.md already exists for exactly this. Keep only: "the block id tracks the invariant text, not SPINE's version; verified byte-identical across launchers."
ACTION: MOVE to provenance.

[COMPRESS 10] Routing-ledger honesty disclaimer (~130 tokens)
ANCHOR: This project has never run that comparison. If you do, we will publish it
WHY SAFE: The operative law is "honesty aid, not proof; no execution receipts exist." The causation paragraph is a grant-apology, not a rule.
ACTION: COMPRESS to two sentences.

[COMPRESS 11] METER LAW clause 4 ruling parenthetical (~70 tokens)
ANCHOR: Boss ruling 2026-08-24, ratifying the fix for the contradiction a council seat raised
WHY SAFE: The operative line ("the line between using a gift and betting on one") precedes it; the ratification narrative is provenance.
ACTION: MOVE parenthetical to provenance.

Q3 STRUCTURE — restructure, but narrowly. The monolith's defense is real for *unconditional* law, and most of Parts I–V is unconditional. But this document already admits three conditional subsystems whose conditions are *observable events, not judgment calls*: a council (requires the boss's go), autonomous hours (requires the boss's declaration), a thin plan card (requires the card to say so). That is the rare case where core-plus-modules does not make compliance optional, because the trigger cannot fire silently. Concretely: always-loaded core = Parts I–V + Principles + fleet test + adjudication + invariant block + posture table/levers + one-line MUST-LOAD triggers. On-demand = council procedure, autonomous discipline, posture case-analysis, episode/visuals mechanics, appendices. The core must contain, before anything else is read: the Ladder of Truth, Principle 3, Principle 6, the dispatch gate, claims-capping, and the three-lists containment rule — containment especially, because the write set must be frozen *before* the build; an orchestrator that looks it up afterward has already lost. What did I, as a first reader, finish without knowing how to do? Compute a meter mark "from the model id" — correctly deferred to wiring. Where did I read twice? Part IV's lineage clauses — the densest passage in the document, and the one most worth keeping in full.

DEFENDED — Part I entire (every task hits Gate-0, the fork, and the contract; the Ladder diagram is the cheapest 6 lines in the file). The ten Principles and abstract roles — definitional, already tight. Part IV's fleet test and lineage clauses — subtle, short, and the failure they prevent (self-review by proxy) is the method's most likely collapse; compressing them is how they get misread. Part V's three-lists containment — must be memorized, never looked up (see above). The posture TABLE and five levers — routing happens every summon. Doctrine 6's Facts≠Flavor — small and load-bearing. The invariant block — it is, in effect, the already-existing minimal core; note the restructure above is just scaling its logic up one level.

TOTAL: ~4,500 tokens recoverable per summon (~2,400 engine + ~1,700 appendices after the notation carve-out), against ~21,600 — roughly 20%.

CONFIDENCE — High on CUTs 1–4 (verbatim duplication, mechanically verifiable). Medium on MOVEs 5–8: they are safe only if the loader enforces the triggers, and I have not seen the loader. What would change my mind: evidence that orchestrators in practice fail to read triggered files (then keep the monolith and take only the CUTs, ~240 tokens), or a usage log showing the posture case-analysis is consulted on most tasks (then MOVE 5 is wrong).