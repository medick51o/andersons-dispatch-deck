cursor-agent exited 0 with no parseable JSON.
stdout: Understood — I'll proceed without locking in a mode. Defaults it is:

- **Vanilla Cursor.** Dispatch Deck stays off unless you say `dispatch on`. I won't spawn `claude.exe`, `agy.exe`, `grok.exe`, Codex, or any second Cursor model unless you name it in a message.
- **Read-first.** When you give me a task, I'll read the real files before proposing anything.
- **No unrequested edits, commits, or PRs.** I build when you ask, ship when you say.

The terms above stand. Seat's taken. What's the first job?
stderr: 