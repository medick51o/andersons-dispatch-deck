cursor-agent exited 0 with no parseable JSON.
stdout: My signed read is at `docs/council-2026-08-23-cursor-bench/answer-kimi-k3.md`. The short version:

**Seat disclosure:** I'm Kimi K3, answering from inside the Cursor pool — the bench arguing its own case, conflict declared.

1. **Triggers** — five legal ones: T1 new-lineage need (Kimi/GLM/Composer are the only non-house blood), T2 1M-context-class artifacts, T3 the effort knob (SPINE's lever 4 is N/A on house seats, live here), T4 LIMP-HOME reserve, T5 speed bursts (weak). Wrong when a house seat clears the bar at $0, when it's curiosity, when it's the same brain wearing a meter, when it's `auto`, or when the pool state is unknown.
2. **Consent** — a standing "bench warrant" on the plan card (named models, read-only, single call) is the only silent-summon class; builds, off-warrant models, and unknown-pool jobs are offer-first; anything near on-demand billing is an explicit ruling.
3. **Ledger** — 10–15 family rows, not 204. Columns: family pattern, seat mark, **lineage (prefix-decided, never host)**, SPINE class, context, effort range, relative cost (never prices), verified-date-or-rumor, best-for/never-for. `cursor-agent models` is the source of truth; three update events only.
4. **Genuinely good for** — ~85% of the pool is your own brains wearing a meter. The real case is: reviews from new lineages (`kimi-k3-high`, `glm-5.2-high`), 1M-tier whole-repo/episode sweeps, Composer speed. Building is the weakest case — long, write-capable, meter-hungry.
5. **Lineage safety** — normative lineage column + borrowed-brain banner extended to 🟣 hosts + a mandatory `builder lineage: X · reviewer lineage: Y` line in every review ticket. `auto` = UNKNOWN LINEAGE, fails closed.
6. **Budget** — the account is the wall: on-demand billing OFF, so the worst case is a dark seat, never a bill. Bench is first-shed, never parallel, 💸 + token footer on every routing-ledger line, warrants die when the plan lapses.
7. **Refuse** — the auto-router. It spends money to kill the routing judgme
stderr: 