I'll read the packet first.I read the full packet: the brief, SPINE.md, SKILL.md, and all five code files. Findings only, in the brief's format.

---

# DELETE LIST — ranked

---

**1. [DELETE] Every CREW/SHOW/tier aside in SPINE.md — all ~15 sites.**

Anchors: `## PART III — THE TEN PRINCIPLES (foundation law, character-free)` and the identical `(character-free)` in Parts IV, V, VI · `## PART VII — REVIEW-CULTURE MECHANICS (character-free; CREW adds the rivalry, SHOW adds the drama)` · `**The abstract roles (CREW/SHOW bind names to these; the Deck uses them plain):**` · Part VI's epigraph `> Operating mechanics for the principles. Higher tiers may bind a presentation-layer name...but SPINE names none.` · `**AUTONOMOUS-HOURS TOKEN DISCIPLINE (the anti-token-inferno core; CREW carries the crew-flavored telling).**` · `CREW binds those colors to CHARACTERS` · `tier legends (Deck SKILL, CREW) are renderings of it` · `Each tier dresses it differently (a plain **panel**, a signed **crew council**, a puppeteered **set-piece**)`.

EVIDENCE: This is the "*this shop's wiring* aside" class that was already cut today, wearing a different hat. Every one of these labels tells the reader which OTHER tier would decorate this law — and the Deck loads no other tier (`This tier loads **SPINE only** — it deliberately does NOT load CREW or SHOW`). A stranger who downloaded only the Deck pays for a guided tour of two products they do not have, on every summon. `**One owner per fact**` already forbids CREW/SHOW from restating SPINE; these asides re-announce that guarantee fifteen times.

COST IF WRONG: A CREW or SHOW author loses the in-engine reminder of which layer owns naming, and could re-state a SPINE law in their own tier. The shop notices as a forked law between tiers — the same failure SKILL.md's stale-legend incident already records, so it is a known and detectable shape.

---

**2. [DELETE] THE NOTATION's decorative and archaeological half.**

Anchors, all inside `## THE NOTATION`: the stray `**` on the line under the heading · `**v4.2. Seat first, act second... (v4.0 repealed the 2026-08-09 marks, including 🟣-as-building.)**` · the `**Grammar scope:**` sentence through `(*🩷⚫ Cassidy (in grok) 📝*)` · the whole `**THE ARROW ➤ BELONGS TO WHOEVER POINTS (v4.2).**` etymology (`The arrow is a **cursor** — that is its birthplace and its meaning`, `the arrow's true home`) and its example line `*🟣➤🌙 💸 Kimi K3 reviewing*` · `**BLOODLINE MARKS for the pool's own families:** 🌙 Moonshot (Kimi) · 🔷 Zhipu (GLM) · 🎼 Cursor (Composer)...` · `**STATES:** 🚩...🟤 quiet hold (watchers armed).` · the parenthetical `(v4.1, rekeyed v2.5 from vendor class to spending, to match THE COUNCIL SEAT LAW)`.

EVIDENCE: The section's own charter is `the marks an orchestrator must PRODUCE, not look up` — and a nine-glyph STATES menagerie (`🪦 retired/parked`, `🟤 quiet hold`, `🕵️ adversary loose`) is a lookup table by any definition; nothing in the method branches on any of them. The repealed-mark history is dead by construction: it exists only to tell you what a mark no longer means. The bloodline marks are per-pool vendor wiring, which SPINE says lives in `SPINE-WIRING.md`, and they are already implemented in code as `BLOODLINE_MARK` in `wmw_cursor_mcp.py` — the seat prints the mark in its own footer. The `📝` crew-grammar carve-out exists because `a LEADING 🔴 is Butch's character color`, i.e. the Deck pays load for a character it never renders. This is the single largest block of non-behavioral text left in the per-summon path.

COST IF WRONG: Narration loses granularity — no glyph for "parked" or "adversary loose." Nothing in dispatch, review, gating or metering changes. The shop notices as flatter status lines, not as a wrong decision.

---

**3. [DELETE] The consent gate's three surplus copies, and the sentence that authorizes them.**

Anchors: in Part I §2, `**This clause OWNS the consent gate. It is deliberately restated at the dispatch gate and THE COUNCIL: a reflex wants redundancy, and the Amendment Law prefers the rule that leaves a trace.**` · in the dispatch gate, `- **An N-way PANEL on one question** (council, bake-off, multi-lens review) — Part I §2 governs: it dispatches only on the boss's **explicit go**, never self-authorized.` · in THE COUNCIL, the paragraph `**Consent gates the convening — offered, never auto-fired.**...that is what makes "opt-in" literally true, in the engine and not just the brochure.` · in THE COUNCIL SEAT LAW, clause `6. **A council is still the SPECIAL move.** Consent to spend is not consent to convene...`.

EVIDENCE: One rule, stated four times in four sections, each naming Part I §2 as the owner. The justification for the redundancy cites **the Amendment Law — which the brief lists as already deleted today.** The load-bearing prose now points at a law a stranger cannot find, so the restatements are literally code supporting something already deleted. Clause 6 of the SEAT LAW adds nothing its own clause 1 did not (`free is not consent to convene: Gate-0's right-size rule still binds (clause 6)` — clause 1 forward-references clause 6 to say what clause 6 says).

COST IF WRONG: An orchestrator reading only the dispatch gate or only THE COUNCIL might self-authorize a panel. This is the most expensive single mistake in the method, which argues for keeping ONE copy at the highest-traffic anchor — not four.

---

**4. [DELETE] Part I §2's fleet-test summary bullet.**

Anchor: `- **A fleet is legal only if all five hold** (the fleet-legality test, Part IV): Declared · Bounded · Accounted · still-Principle-3 · Authority-inheritance. A fleet nobody declared, bounded, or counted is banned.`

EVIDENCE: It is a summary of Part IV that has already rotted. Part IV lists **seven** terms — Declared, Bounded, **Destined**, Accounted, **Governed where it RUNS**, Still Principle 3, Authority inheritance — and the Part I copy silently drops the two that cost real money: *Destined* (`An agent with no destination still spends at full rate`, the exact Aug 21–22 failure the whole `preflight` function exists for) and *Governed where it RUNS* (the vendor-side lanes). A reader who trusts the summary is missing the finding that drove the code. (Note as evidence of neglect, not as a fix request: Part IV's own header also reads `all five hold` above a seven-item list.)

COST IF WRONG: Part I §2 loses a one-line reminder that fleets are governed. Part IV is one screen away and is the version that is actually correct.

---

**5. [DELETE] The "X owns this" pointers that then re-teach X.**

Anchors: Part VI's bullet `- **Probe the TRANSPORT, not the binary** (THE TRANSPORT LAW owns this): a seat is online when its persistent seat answers in THIS session. A CLI `--version` proves only that the fallback lane exists...` (THE TRANSPORT LAW clause 4 states this in full, three lines, and the two clauses cite each other) · Principle 5's `**Gates referee, but a gate is only an arbiter if it can FAIL** (Ladder of Truth, Part I §1, which owns the oracle check and the RED-first rule)` · Part V clause `4. **Claims are capped at what a model can prove.** "Gates pass," never "it works." (Ladder of Truth.)` · in Review dispatch, `(preferred — different weights/training/context; a different account merely hosting the builder's own brain does NOT count, see the effective-model preflight)` — a third verbatim copy of a sentence already in Part IV's anti-laundering guard and in the preflight itself · THE TRANSPORT LAW clause 2's second half, `Fresh alone does not make a review independent — Part IV's two legal paths still bind (different effective-model vendor outside the build's lineage, or a boss-launched fresh-context seat).`

EVIDENCE: Naming an owner and then restating the owned rule is the worst of both patterns: it pays the tokens of duplication *and* advertises that it is duplicating. Part V §4 is the purest case — one sentence, a citation, and zero content the Ladder does not already carry. These are the "rules stated twice" the brief predicted more of.

COST IF WRONG: A reader who only opens one Part follows the pointer instead of finding the rule inline — one extra hop, no lost law. The mutual `--version` pair is the safest of all: whichever copy survives says the same thing.

---

**6. [DELETE] The episode folder's ENDING CREDITS exception and the screenshot-compression recipe.**

Anchors: `**One sanctioned exception (boss amendment, same day): the ENDING CREDITS — show tiers only.**` through `**The dispatch deck does NOT roll credits** — the plain tier closes plainly` · the whole `**Visuals: the boss's screenshots are reality evidence — file them, cheaply.**` paragraph, in particular `RE-COMPRESSED to economical JPEG (cap ~1280px on the long edge, quality ~70; a full-HD PNG becomes a small JPG)` and `(Mechanics: uploads arrive under `.claude\uploads\` — convert on copy with whatever image tool the box has; ffmpeg and Pillow both do it in one line.)`.

EVIDENCE: The credits rule is a SHOW-tier presentation law that explicitly exempts the only tier loading it, and it carries a `(boss amendment, same day)` attribution — the class already purged. The visuals paragraph is image-encoding wiring (a pixel cap, a JPEG quality number, two library names, a vendor upload path) inside a document whose own rule is `Wiring... is CODE, not prose` and `Methodology is not law`. It also instructs `Zero ceremony` in nine lines of ceremony.

COST IF WRONG: Screenshots get filed at full size, costing disk. The evidentiary rule that matters — screenshots are reality evidence, file them under the episode — survives in its first sentence.

---

**7. [DELETE] SPINE's preamble tier-tour and mission statement.**

Anchors: lines 8–10, `The Deck renders this plain; TRM (CREW) adds a crew on top; TRTO (SHOW) adds a story on top. **Neither CREW nor SHOW restates SPINE.** Edit the method once, here, and all three tiers inherit it.` · lines 12–16, `**What this engine is (brand-neutral):** a discipline for structured collaboration between one orchestrator...Keeping a model aligned with the human is a separate discipline (the Anderson Method house rules).` · line 5, `Git and the versioned owner headings below carry the history; each law is owned by its own section.`

EVIDENCE: Ten lines before the first law, none of it instructing anyone to do anything. The brand-neutral paragraph is a product description; the tier tour is item 1's disease at the top of the file; the Git sentence describes the repository. `**One owner per fact.**` and `**Any content change bumps this line**` are the two operative sentences in this block and both stand alone.

COST IF WRONG: A first-time reader loses the framing paragraph and starts at THE LADDER OF TRUTH. Given the brief's cost function, a stranger reaching load-bearing law ten lines sooner is the point.

---

**8. [DELETE] SKILL.md `## ON INVOCATION` step 2's body.**

Anchor: everything after `2. **PROBE the arsenal, don't assume it**` — from `**Probe the TRANSPORT first** (SPINE v2.0 transport law #4): a seat is online when...` through `...unreviewed work is never reported as done.`

EVIDENCE: Eleven lines re-teaching SPINE Part VI's *Reachability & effective-model preflight* almost clause for clause — transport-before-binary, effective model behind the host, `UNKNOWN LINEAGE` failing closed, the three independence statuses, the declare-in-one-line format — in the file that states `**This file does not restate it.**` two sections earlier. It also hardcodes a stale citation (`SPINE v2.0 transport law #4` against a v2.8 dependency), which is exactly the drift the same file's legend post-mortem is about. Step 2's first clause plus the pointer already dispatches the reader correctly.

COST IF WRONG: The loader stops carrying an offline copy of the preflight. If SPINE fails to load, the run has already halted by the DEPENDS check, so there is no scenario where this copy is the only one present.

---

**9. [DELETE] armcheck.py's reserve-path canary and its dead pointer.**

Anchors: `RESV = os.path.join(os.path.expanduser("~"), ".anderson-method", "reservations.json")` · the comment block `# --- Kimi's exploit pass, 2026-08-24. The guard path was DEAD CODE (NameError on every guarded write dispatch)...This canary exercises the reserve path itself.` · the `if DEEP:` block ending `check("cursor: the guarded write path RUNS (no NameError in reserve)", "NameError" not in _gt and "is not defined" not in _gt)`.

EVIDENCE: `RESV` is assigned and never read — a path to the deleted reservation store. The canary's stated purpose is `the ONLY test that reaches the reserve path`, and the reserve path was deleted today: in `wmw_cursor_mcp.py` the guard block now contains preflight and nothing else. So this check spends a real `--deep` dispatch to prove that deleted code raises no `NameError`, which it cannot do. A guard that cannot fail, paid for in tokens, guarding something that no longer exists — all three of the brief's hunting categories at once.

COST IF WRONG: The write-capable path loses its only live smoke test. Note honestly: that is a genuine loss of coverage on the `always_approve` lane, but the coverage it provides today is a tautology; whatever replaces it is a new test, which is out of scope here.

---

**10. [DELETE] dispatch-guard.py's reservation remnants.**

Anchors: `STORE = os.environ.get("WMW_GUARD_FILE", ...reservations.json)` · `LOCK = STORE + ".lock"` · `MAX_CONCURRENT` · `LEASE_TTL_MIN` · `LOCK_STALE_S` · `MAX_SINGLE_CLAIM_PCT` and its comment `# a dispatch may not claim more than this share of the month in one go` · `MAX_OUTSTANDING_PCT` · `import time` · the orphan banners `# ---- locking` and `# ---- reservation` · in the docstring, the paragraph `A reservation subsystem also lived here and was DELETED 2026-08-24 by a council vote...Concurrency is capped by the vendor, or not here.` and the attribution block `Two findings drove this, neither of them mine: Boss — ... Kimi — ...`.

EVIDENCE: Seven module constants and one import with zero readers after the reservation cut — `MAX_CONCURRENT`, `LEASE_TTL_MIN`, `LOCK_STALE_S`, `MAX_SINGLE_CLAIM_PCT`, `MAX_OUTSTANDING_PCT`, `STORE`, `LOCK`, `time` appear nowhere in `preflight`, `yield_report`, or `main`. Worse, five of them are `os.environ` reads, so `WMW_MAX_CONCURRENT_JOBS` and `WMW_MAX_SINGLE_CLAIM_PCT` still look like live controls to any operator who sets them, and silently do nothing. The docstring eulogy and dated attributions print as `argparse` help text.

COST IF WRONG: Nothing executes differently — this is provably unreachable configuration. If concurrency capping is ever rebuilt, the constants are one commit away in git.

---

**11. [DELETE] read-meters.py's references to the deleted bench-burn.py.**

Anchors: in the docstring, `This tool reads the percentage so that measurement is possible; `bench-burn.py` reports what a burn cost.` · in `main()`, `print("  burn a known amount and watch the percentage move (see bench-burn.py).")`.

EVIDENCE: `bench-burn.py` is on the brief's already-deleted list. The second reference is printed to the operator on every single meter read — the tool's closing line instructs them to consult a file that does not exist, while `calibrate-pool.py`, which actually performs that measurement, is never named.

COST IF WRONG: None to behavior. If the pointer is simply removed, the operator loses a lead they could not have followed anyway.

---

**12. [DELETE] The dated audit scars in code comments — the purge stopped at the prose.**

Anchors (representative, all in-code): `wmw_cursor_mcp.py` — `# (Audit 2026-08-24, Kimi finding 7, CONFIRMED.)`, `# (Audit 2026-08-24, Kimi, CONFIRMED gap.)`, `# (Audit 2026-08-24, Kimi finding 10.)`, `# (Audit 2026-08-24, two seats.)`, `# reproduced on the Grok seat 2026-08-23. The old mitigation was "just don't put writable servers in that file"...`, and the `# THE COUNCIL LOCK (boss ruling 2026-08-23, revisitable)` header block · `armcheck.py` — `# --- the Gemini seat, audited 2026-08-24. Every one of these was LEGAL before. ---`, `# (Codex audit 2026-08-24, CONFIRMED HIGH.)` ×2, and the six-line `# 2b. THE BEHAVIOURAL CANARIES (added 2026-08-24, after an audit found this suite was ENTIRELY argument-validation...)` header · `dispatch-guard.py` — `# Reading the old path made this report a confident zero. (Codex, 2026-08-24.)`, `# Expecting the wrong shape made every row count as zero tokens.` · `preflight`'s user-facing string `(this is the Aug 21-22 failure, exactly)` · `allowance.py` — `(Audit 2026-08-24, Kimi, CONFIRMED logic bug.)` · `wmw_cursor_mcp.py` docstring `SECURITY (v2.0 — after a live command-injection reproduction on this machine)`.

EVIDENCE: The brief says `every dated scar, (boss ruling YYYY-MM-DD) attribution` was already cut — but that purge ran on SPINE and SKILL.md only. In the code these are now the dominant form of comment; in `armcheck.py` the audit narration is longer than the tests. Every one of them dates a single Sunday, so within a month they read as archaeology, and the who-found-it credit is the class already ruled out of the method. The invariant each one guards is separable from the story: *what* must hold is worth a comment, *who noticed on which date* is git's job.

COST IF WRONG: Real, and I want it named plainly — several of these comments encode a **why-not** that the code cannot show (why the ledger lives outside the playpen, why `--approve-mcps` is confined to the write path, why a false pass is possible when a seat never answers). Losing the *reason* would be a genuine regression; losing the *date and the finder's name* costs nothing. If the council cannot separate the two in one pass, keep the reason and drop the citation.

---

**13. [DELETE] SKILL.md's self-referential and duplicated delta prose.**

Anchors: the whole `### THE LEGEND` body from `A forked legend is how the tiers drift: this file carried a stale v4.0 against SPINE's v4.2 for two days...` through `Both of those were repealed marks being rendered on live lines.` · in `## WHAT THE DECK ADDS ON TOP OF SPINE`, the sentences `The Deck adds nothing to the *method*. Its entire delta is **plain rendering + the gold-baton color narration.** Every rule below is SPINE's; this section only says how the Deck *presents* it.` (the same paragraph opens with `**The Deck adds NOTHING to the method.**`) · in the YAML `description`, `Reserved rebrand alias: 'Agentic Dispatch Director' (also ADD).` · in the body, `no cat, no Jessie/James/Butch/Cassidy, no episodes, no "prepare for trouble."` · in `## LOAD RECEIPT`, `If a dep is stale, the receipt says so and the run stops.`

EVIDENCE: The legend section spends five lines narrating its own two-day bug; the operative rule is its first sentence, `**Read THE NOTATION in SPINE and render it plain**`. "The Deck adds nothing" is stated twice in one paragraph and twice more downstream (`The color is a status light, not a costume`, `The one thing this tier adds is the **gold baton**`). The reserved alias is a brand nobody has used, in always-loaded frontmatter. The character list makes the no-characters tier the only place in the shop that teaches the characters' names. The stale-dep clause repeats `**HALT and tell the boss**` from `## DEPENDENCIES` nine lines up.

COST IF WRONG: Nothing. Every deleted sentence has a surviving twin in the same file.

---

**14. [DELETE] SKILL.md `### FUEL MODE — opt-in ADHD verbiage register` in full.**

EVIDENCE: Fourteen lines paid on every summon by every reader, to enable a register that is off by default, must be turned on by name each session (`It is never on unless THIS session's boss turned it on`), and — by its own statement — changes nothing: `**Verbiage only.** The register never touches routing, verdicts, evidence rank, tickets, or reports`. It is also the one place in the engine encoding a specific person's neurology (`the boss's brain runs on an interest-based nervous system`, `psychological reactance`), which a stranger downloading the method inherits and cannot use.

COST IF WRONG: This is the item I am least comfortable naming, and I am naming it anyway because the pull to keep it is affection, not utility. If it is deleted, the boss loses a register he likes and would have to re-describe it. Nothing about dispatch, cost or review degrades — the file itself guarantees that.

---

**15. [DELETE] Version archaeology outside THE NOTATION.**

Anchors: Part VIII's entire `*Note on the block id: the `v2026-07-22 r2` inside the block is the invariant block's own identity...bumped r1 → r2 on 2026-07-22, when "another vendor's account" was tightened to "a different effective-model vendor and lineage"...do not change it to match a spine version.*` · Part I §2's `(an external multi-agent writeup measured ~15x the tokens of a single chat — their number, not a law of nature; the gate exists because of that shape)` · the routing bullet's `(a heuristic, not a measured result)`.

EVIDENCE: The block-id note is eight lines of version history for a four-line constant, and its whole instruction — do not renumber it — is already carried by `copy it VERBATIM` in the sentence above. The 15x citation is a third-party number the text immediately disowns twice (`their number, not a law of nature`); `**Earn-a-head:** every added seat must be justifiable in one sentence` is the rule, and it does not rest on the number. `(a heuristic, not a measured result)` disclaims a sentence into weightlessness.

COST IF WRONG: Someone eventually renumbers the invariant block to match SPINE's version, breaking the byte-identity check across four files. That is a real failure mode — but it is prevented by the word VERBATIM, not by the history of the r1→r2 bump.

---

**16. [DELETE] Derivable-corollary sections: Part IV `**Continuity.**`, `### Routing under a thin budget`'s duplicated sentences, and THE COUNCIL's descriptive padding.**

Anchors: `**Continuity.** If a seat goes dark mid-mission...succession never converts unapproved work into fresh-eyes material.` · in Routing under a thin budget, `Route among seats that clear the quality bar, then prefer the fuller tank.` and `Pretending a mid-tier seat is frontier does not save money, it lowers the bar.` (both are Principle 7: `prefer the billing you have headroom on` and `Economics picks among the seats that clear the bar — it never lowers the bar`) · in THE COUNCIL, `**When NOT to convene.** Gate-0 binds absolutely...The orchestrator does not *oops* into a token-eating dream team for a two-line task.` and the closing paragraph `Adversarial verification at full width — Part IV's review law scaled to N independent perspectives...**The council widens coverage; it never replaces in-hand validation.**` and the second `"as many as it takes" is not a number` (Part IV's `**Bounded.**` owns it).

EVIDENCE: Continuity's operative content is `**Authority inheritance**` applied to succession instead of spawning, which the preceding bullet's `in full` already covers; its first clause also sits awkwardly against Principle 10 (`A blocked seat re-plans around the block`). Routing-under-a-thin-budget is three sentences, two of which are Principle 7 restated, leaving `**Review coverage is never the thing you cut**` — which is the only line in the section that must survive. THE COUNCIL's *When NOT to convene* announces that Gate-0 binds, which is Gate-0's job, and the closing paragraph is pure description of what the reader just read.

COST IF WRONG: Edge cases get inferred instead of quoted — a successor seat's restrictions, a thin-budget tiebreak. Both inferences run in the safe direction, toward Principle 3 and toward the fuller tank.

---

**17. [DELETE] calibrate-pool.py.**

EVIDENCE: A tool whose default invocation (`--calls 6`) makes six real dispatches to spend the operator's allowance on purpose (`This spends real allowance on purpose`) in order to compute a number the shop has already recorded in `MEASURING-POOLS.md`. Its partner in that measurement, `bench-burn.py`, was deleted this morning as methodology; the brief's own standard is `Methodology is not law`. The owner's stated fear is a stranger burning their month on this shop's rig — and this is the only file in the packet that burns a month's allowance by design, sitting one curious command away in a repo they just cloned.

COST IF WRONG: THE METER LAW clause 2 (`Measure, never infer`) becomes un-executable — the next time a vendor resizes a pool, nobody can size it without rewriting this script. That is a genuine cost, and it is why I rank this last rather than higher.

---

# DEFEND — what must NOT go

- **Principle 3 and Part IV's anti-laundering guard, paths (a) and (b).** The one fixed point. Everything else in the method is negotiable; this is what makes the review adversarial rather than decorative.
- **Part V §5, the three lists and the containment rule** (`actual delta ⊆ write set` **and** `actual delta ⊆ review manifest`, hashes computed by the reviewer `from the bytes it was given`). This is the only mechanism in the entire engine that does not take a builder's word for anything, it is checkable by a human in ten seconds, and it costs almost nothing to load. Cut anything else first.
- **Part V §6's `**Untestability is never evidence against a finding.**`** The single sentence standing between this method and a shop that ranks every hard finding NOT PROVEN.
- **The dispatch gate's two questions and `**Both no → just do it**`.** The largest token saving in the document is the rule that says don't orchestrate. Whatever else is trimmed, this earns its lines many times over.
- **One copy of the consent gate**, and `**Earn-a-head**`. The panel is the most expensive move in the method; the requirement to ask is the brake.
- **`METER MARKS ARE MANDATORY ON ANY LINE THAT CAN SPEND`** and its `♾️ / ♾️💸 / 💸 / 🚨💳 / ⚠️` mark set, plus `**⚠️** unknown, which fails closed`. This is the owner's own cost anxiety implemented as a narration duty, and the code already computes it. Item 2 deletes the glyphs around this clause and deliberately not this clause.
- **In `wmw_cursor_mcp.py`: `meter_class` and its `UNKNOWN` refusal, the CREDIT GUARD, the allowance check, `yolo_allowed`, and `_guard()` returning the exception so `always_approve` fails closed.** These are the four places money or write authority is stopped *before* it moves, and the fail-closed guard loader is the one control that survives its own file breaking.
- **The prompt spill-to-file transport, `_MODEL_RE`, `_UUID_RE`, `_safe_id`, `_safe_model`, and `MAX_FRAME`.** The audit is right that `cwd` is not an OS boundary and that most of this harness is accident-prevention rather than security — but these are the exception. They defeat a *reproduced* command injection through the Windows `.cmd` shim, and the attacker there is a crafted prompt, not a co-equal process. Also keep `--approve-mcps` confined to the write-capable path: a read-only seat that can drive a writable neighbour has no read-only mode, and that was reproduced too.
- **`_safe_cwd`'s secret-directory list.** I checked whether it was subsumed by the HOME and APPDATA rules: it is not. Those match roots by *equality*, so `cwd=~/.ssh` is caught only by this list.
- **`dispatch-guard.preflight` and its wiring into the write path.** The only control in the shop tied to a measured loss (`11 of 13 produced zero lines`), and `**Destined**` in Part IV is its law. Keep both.
- **`read-meters.py` entirely** (minus item 11's dangling pointer), including the undocumented endpoint documentation. It reads reality, spends nothing, and its endpoints are the kind of gotcha a fresh install cannot rediscover.
- **The behavioural canaries' false-pass fix** — `_answered and not _breached(_tag)` with `"seat never answered - attack never landed"`. A guard test that passes when the attack never landed is worse than no test; this is the correction, not the ceremony.
- **Principle 1's `a signature identifies the **declared** seat, not a verified model`** and `The signature makes identity **legible and falsifiable**, not proven.` Given that an audit already called this harness false assurance, the honest labeling of a control's limits is the most valuable prose in Part III. Deleting it converts a legible control into a claimed one.
- **`**Review coverage is never the thing you cut**`** — the survivor of item 16's third bullet.
- **Principle 10 and phone-readable mission reports.** The method's user is on a phone; a rule that keeps plans from silently requiring his hands is load-bearing, not courtesy.

---

# THE ONE THING

**THE NOTATION's decorative and archaeological half (item 2).**

If I get one delete, I take the largest block of text in the per-summon path that no decision depends on. Nothing in dispatch, routing, review, adjudication, or metering branches on `🪦`, `🟤`, `🕵️`, `🎼`, or on the etymology of an arrow. It is a glyph dictionary in a section whose own charter says it is not a lookup, it carries repealed-mark history that exists only to describe what a mark no longer means, it holds per-pool vendor wiring that SPINE says lives in `SPINE-WIRING.md` and that the seat wrapper already computes and prints itself, and it teaches the Deck the crew-color grammar of a tier the Deck refuses to load.

I chose it because it is the item I felt the strongest pull to approve. It is the shop's handwriting — it is fun, it is identity, and four councils in one day left it alone while cutting laws. That is exactly the signature the brief sent me to look for: not a rule anybody defended, a block nobody examined.

---

# CONFIDENCE

**High** on items 4, 9, 10, 11, and 13 — these are checkable facts, not judgments. I verified each against the packet: `MAX_CONCURRENT`, `LEASE_TTL_MIN`, `LOCK_STALE_S`, `MAX_SINGLE_CLAIM_PCT`, `MAX_OUTSTANDING_PCT`, `STORE`, `LOCK`, `import time` and `RESV` have no readers; the reserve path the `--deep` canary tests is absent from the guard block it points at; `bench-burn.py` is on your own deleted list and is printed to stdout on every meter read; Part IV lists seven terms under the words "all five" and the Part I summary drops the two that cost money; the "Deck adds nothing" sentence appears twice in one paragraph.

**High** on items 1, 2, 3, 5, 6, and 7 as *duplication and description*; **medium** on whether you want them gone. Item 3 has a structural argument I would press: its own justification cites the Amendment Law, which you deleted this morning, so the clause authorizing the redundancy no longer has a law behind it.

**Medium** on 12, 14, 15, and 16. Item 12 needs the split I named — reasons stay, dates and finder credits go — and I would not want a mechanical purge to take the *why-nots* with them.

**Low, and deliberately submitted anyway** on 17. Under your decision rule a single vote changes nothing, and this is the one file in the packet that spends a stranger's month by design, so it should be on the record even if it stays.

**On my standing:** I am a labeled degraded self-check, same lineage as the author, and I found the pull to approve concentrated in exactly three places — THE NOTATION, THE COUNCIL, and FUEL MODE. All three are identity rather than method, and I named all three. Where I agreed with the existing text, that is weak evidence and I have said so in the DEFEND section rather than pretending it is independent confirmation. Two places I checked expecting to find ceremony and found a real guard instead — `_safe_cwd`'s secret list and the prompt spill transport — are reported as defends, not deletes.