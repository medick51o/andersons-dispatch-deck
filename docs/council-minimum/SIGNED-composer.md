I'll read the brief in full and answer only what it asks for, in its format.
```
STILL SHOP HOMEWORK — anchored list

SPINE
- Version/history stamps, not stranger law: `spine v2.8 (2026-08-24)` (lines 90–92); invariant-block continuity note `bumped r1 → r2 on 2026-07-22` (lines 547–553).
- Boss ruling dates as provenance: `boss ruling 2026-07-24` on ticket LAWS line and episode style (lines 347–348, 357–358); `boss ruling 2026-08-24` on Meter Law subsidy + Council Seat Law rewrite (lines 566, 574); `boss-adopted 2026-08-23` on NOTATION v4.2 (lines 631–633); `boss amendment, same day` on ending credits (lines 360–361).
- Tier/theater homework (Deck is character-free): CREW/SHOW inheritance, character names, credits, crew 🔴/📝 split — e.g. `Neither CREW nor SHOW restates SPINE` (lines 9–10); `Butch's character color` / `🩷⚫ Cassidy (in grok) 📝` (lines 639–641); `filmed on location · 2026-07-23 → 2026-07-24` (lines 362–363); TRM/SHOW episode naming vs backend dates (lines 357–365).
- This shop's measured/incident scars: `~15x the tokens of a single chat — their number, not a law of nature` (lines 71–72); `Practical scars: when the reviewer can't read the repo…` three-flips anecdote (lines 468–476); `A forked legend is how the tiers drift: this file carried a stale v4.0 against SPINE's v4.2 for two days` (SKILL lines 833–837, embedded 51–54).
- Shop wiring pointers and paths (obligation to read elsewhere is law; contents are homework): `the shop's wiring (`SPINE-WIRING.md`), not the engine's law` (lines 407); `WIRING & FIELD NOTES — NOT loaded on a summon` + trigger list (lines 676–690); `mcp-seats/allowance.py` (lines 599–601); `MEASURING-POOLS.md` and `docs/` methodology pointer (lines 571–572).
- Episode backend mechanics: `episodes/YYYY-MM-DD-<slug>/` harvest/bottling lane (lines 352–356); screenshot JPEG recompress spec + `.claude\uploads\` + ffmpeg/Pillow (lines 367–374).
- Anderson-house / boss-life specifics: `Keeping a model aligned with the human is a separate discipline (the Anderson Method house rules)` (lines 16); `he is usually on a phone` (lines 142–143); `While the boss IS reachable… a sleeping boss` unattended exception (lines 519–522).
- Vendor-install scar tissue in law prose: Antigravity/Gemini host example, Overflow Valve, spend-limit vs allowance, cloud-agent control-plane note (lines 179–181, 316–317); TRANSPORT LAW `added v2.0, 2026-08-22` (lines 603–608).

SKILL (loader — strangers load this too)
- Shop path and install scar: `Wrapper source: C:\Sync\Projects\andersons-dispatch-deck\mcp-seats\` (lines 867–868); `installed & verified 2026-08-22` (line 874); `the two headless croak-killers found 2026-08-22: a 60-minute timeout… always_approve` (lines 885–887).
- Standing MCP table (this shop's servers/tools): `wmw-codex` / `codex` / `codex-reply`, `wmw-grok`, `wmw-gemini`, Antigravity `agy` (lines 861–866, 878–885) — wiring, not universal law.
- Shop fit-routing illustration, explicitly not new law: `This shop's wiring (`SPINE-WIRING.md`), as an ILLUSTRATION… Codex is usually the sharpest CODE reviewer` (lines 924–927); `the green seat (Gemini, via Antigravity)… Overflow Valve` (lines 934–937).
- Cut artifact still referenced: `bench-burn.py for the readings themselves` (lines 909–910) — homework pointer to removed load.
- Boss ADHD register: `FUEL MODE — opt-in ADHD verbiage register (boss-adopted 2026-08-24; OFF by default)` + psychological reactance prose (lines 859–872).
- Rebrand/date trivia: `Reserved future rebrand, coined 2026-07-17: "Agentic Dispatch Director"` (lines 789–790); `claude mcp list` probe detail (lines 966–967).

CEREMONY — anchored list

SPINE
- Dispatch gate duplicated: Part I §2 (lines 141–158) restated wholesale in Part VI `The dispatch gate (before every task)` (lines 279–286).
- Council consent gate triple-stated: Part I §2 `PROPOSE a panel… explicit go` (lines 153–158); Part VI panel bullet (lines 285–286); THE COUNCIL `Consent gates the convening` (lines 430–434) + Council Seat Law clause 6 (lines 592–593).
- `Gates pass, never "it works"` / claims capped: Ladder (lines 34–35), Part V §4 (line 227), mission reports (lines 465–466), canonical block (lines 540–541), Principle 5 echo (lines 212–214).
- Builder ≠ reviewer restated beyond Principle 3: Part I dispatch gate (lines 158–160), fleet test (lines 182–186), anti-laundering guard (lines 197–204), review dispatch (lines 399–418), invariants (lines 538–539).
- Gate oracle / RED-first / untested test opinion: Ladder (lines 127–134) echoed in Principle 5 pointer (lines 212–214) and Part V dispute escalation (lines 252–254).
- `Ambiguity is a finding` + ticket line `Propose, don't guess` duplicate (lines 47–49, 349–350).
- Autonomous-hours bell duplicates Principle 8 units: Part VII `two rounds per debate` (lines 509–513) vs Principle 8 (lines 228–233).
- Abstract roles paragraph (lines 148–158) after ten principles already define orchestrator/builder/reviewer/boss.
- Part VI header `Higher tiers may bind a presentation-layer name` (lines 274–277) — meta, not operative for strangers.
- Review-culture tone rules (suggested fix on every finding, nits cap, grade work not worker) — competent review practice (lines 494–501).
- Signature mechanic tier renderings beyond `every seat signs` (lines 528–531) — Deck-only strangers don't need CREW color binding prose.
- NOTATION crew-tier rendering exceptions (🔴 vs 📝, 👑 combos) for plain Deck strangers (lines 639–659) — ceremony for this tier.
- Meter mark full glyph taxonomy narrated twice (NOTATION lines 663–670; SKILL reserve section lines 904–905) — competent orchestrators announce spend; emoji ledger is shop voice.

SKILL
- Entire `NON-NEGOTIABLES` card restates SPINE with `this card names the guardrail, it does not restate the rule` (lines 952–960) — explicit duplicate.
- `PERSISTENT SEATS` transport bullets re-copy THE TRANSPORT LAW already in SPINE (lines 889–901 vs SPINE 610–626).
- `RUNNING THE DECK` 6-step checklist duplicates Part I + Part VI mechanics (lines 928–950).
- `NARRATE IN COLOR` example paragraph (lines 841–846) — illustration, not new law.
- `THE LEGEND — rendered, never restated` section that mostly recounts the stale-v4.0 incident (lines 848–857).
- `ON INVOCATION` step 3 repeats step 2's output requirements in prose (lines 962–977).
- Verbatim invariant block copy #2 (lines 979–989) when SPINE Part VIII already owns it (lines 536–545) — necessary for entry-file Principle 9, redundant inside summon load if SPINE is already loaded.

THE 5,000-TOKEN ENGINE — what survives, specifically

1. **Invariant block (verbatim)** — four bullets (SPINE lines 536–545): builder lineage ≠ reviewer; evidence-capped claims; disputes go up; signed seats; boss merges.
2. **Gate-0** — two dispatch questions; default lean (one builder + one cross-vendor reviewer); panel/council only on boss explicit go; earn-a-head one sentence (Part I §2, compressed).
3. **Ladder of Truth (compressed)** — rung order; vendor claims ≠ gates; gate must fail; red-before-green for regressions; boss in-hand tops bench; ambiguity → finding, not build (Part I §1 core bullets).
4. **Principle 3 + effective-model preflight** — independence = different effective-model vendor+lineage OR boss-launched fresh seat; probe reachability + effective model, not CLI name; `UNKNOWN LINEAGE` → fail closed, `REVIEW UNAVAILABLE`; output status line `FULL CROSS-VENDOR` / `SOLO-VENDOR DEGRADED` / `REVIEW UNAVAILABLE` (Part IV anti-laundering + Part VI preflight, stripped of examples).
5. **Fleet legality (five + principle 3)** — declared, bounded (atomic), destined, accounted, governed where it runs; charter-declared seat lineage when orchestrator launches (Part IV, no cloud-vendor essay).
6. **Diagnose/Design fork** — bug → instrument; novel/gnarly → propose council (consent-gated); re-classify when shape changes (Part I §3).
7. **Reality contract (five terms, one line each)** — observable outcome, instrument signal, protected invariants, rollback, boss handover test-kit (Part I §4 table, no prose expansion).
8. **Ticket contract** — TASK (boss verbatim on reviews), EXPECTED OUTCOME, CONTEXT paths, CONSTRAINTS, MUST DO (+ verify command), MUST NOT, WRITE SET mandatory, OUTPUT FORMAT; load-bearing ambiguity line (Part VI Tickets).
9. **Review dispatch** — reviewer gets: original task verbatim; full write set uncurated; diff + acceptance; verify output; never builder reasoning; adversarial channel last to fail (Part VI Review dispatch).
10. **Three lists containment** — write set (frozen pre-build), actual delta from `git diff`+`git status`, review manifest with reviewer-computed hashes; `delta ⊆ write set` and `delta ⊆ manifest`; breaches → INCOMPLETE (Part V §5).
11. **Adjudication ladder** — per-finding ACCEPT/DISPUTE; BLOCKER/MATERIAL/MINOR/NOT PROVEN; repairs get fresh review; cap TWO ROUNDS then boss fork (Part V §1–3, §6 tail + Principle 8 review-dispute cap only).
12. **Routing classes** — FRONTIER / WORKHORSE / FAST by judgment content; cheapest seat that clears bar; thin budget cuts builds/fan-outs, never review (Part VI routing table + thin budget, no dated model IDs).
13. **Council procedure skeleton** — special move; propose why+cost; brief → distinct lenses → parallel signed reads → synthesis with attributed disagreements → cap → boss rules (THE COUNCIL steps 1–6, no "when NOT to convene" essay).
14. **Council Seat Law (spend gate only)** — seat that can spend needs recorded allowance before convening; past allowance re-ask; unknown cost fails closed; allowance ≠ consent to convene (clauses 2–6, no `allowance.py` path).
15. **Transport law (minimum)** — default persistent seat sessions; fresh call for reviewers; reply-chain stays in owning lineage; preflight probes transport in this session, not `--version` alone; one-shot CLI = fallback (THE TRANSPORT LAW 1–5, no install/remove prose).
16. **Meter Law (minimum)** — readable spend; measure don't infer; subsidy never sole path; meter output not just input (Meter Law 1–4, no MEASURING-POOLS pointer body).
17. **Notation (Deck-minimum)** — seat-first: 🔨 build, 🔴 review (Deck), ⛔ blocked, 🟡➤ conductor, 🟣➤ reserve transport, bare 🟣 answer; mandatory meter glyph on any line that can spend; ⚠️ unknown fails closed (NOTATION core bullets only).
18. **Mission report shape** — phone-readable; outcome first; per-seat one-liners; concrete ruling options; cost note on fan-out; evidence-capped claims (Part VI Mission reports + Principle 10 one-liner).
19. **Wiring obligation (pointer only)** — before first preflight/dispatch, load `SPINE-WIRING.md` for arsenal strings, gotchas, allowance location; contents not law (lines 676–690, one paragraph).
20. **SKILL loader (~800 tokens)** — DEPENDS SPINE ≥ floor; halt if missing/stale; load receipt `🟡➤ ADD loaded · spine <parsed>`; ON INVOCATION: load SPINE → load wiring → probe transport + effective models → declare status → `What's the job?`; plain model names, no persona; optional one-line color narration rule.

WHAT I'D REGRET

- **Review-culture checkpoint mechanics** (Part VII) — losing non-blocking vs BLOCKER vs emergency brake distinctions makes mid-lane reviews either too chatty or too halt-happy; strangers will argue on the line or miss damning finds.
- **Dispute escalation detail** (Part V §6) — without oracle-approval and "unt testable ≠ NOT PROVEN," reviewers rank real blockers down or builders weaponize missing tests; quality of adversarial review drops materially.
- **Episode/documentation lane** — losing write-set baseline snapshot + episode folder obligation hurts auditability and the three-lists baseline; containment still works per-mission but harvest/replay for the boss gets worse.
- **Destined + governed-where-it-runs fleet clauses** — fan-outs that bill with empty diffs and cloud agents ignoring repo config; cost burns before work lands.
- **Reality contract depth** — five one-liners survive but lose rollback voice (`15/16 landed, #16 reverted-and-flagged`) and instrument-vs-account distinction; more "done" claims without boss test-kit discipline.
- **NOTATION meter taxonomy** — bare ⚠️ fails closed survives, but losing ♾️/💸/🚨💳 forces vaguer spend narration; boss learns cost from footnotes.
- **Autonomous-hours decision queue + pivot rules** — unattended runs re-litigate past the bell; token inferno risk rises even with round caps stated.
- **Charter-declared seat lineage nuance** — orchestrator-hosted builders treated as one lineage with orchestrator; without it, internal review looks legal when it isn't.

Severity: **Principle 3 + three lists + preflight fail-closed + ticket/review dispatch** are the load-bearing core — cutting below those would break "amazing" into "busy theater." The regrets above mostly erode cost control, unattended discipline, and review sharpness, not the basic summon→build→review→gate spine.

CONFIDENCE

**Medium-high** on homework vs ceremony splits (many items are explicitly labeled shop/wiring/not-loaded in SPINE itself). **Medium** on the exact 5,000-token packing list — survivable set is clear; token budget allocation per section would need measurement. **High** that per-summon load should be invariant block + Gate-0 + Principle 3/preflight + tickets + three lists + review dispatch + transport/Meter/Council spend gates + thin SKILL invocation, with everything dated, scarred, tier-theater, MCP table, fit-routing illustration, Fuel Mode, and duplicate guardrail cards moved to docs-only.
```