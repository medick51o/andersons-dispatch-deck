I'll read the packet in full and follow it exactly, starting with the brief and any skills it requires.The packet is large, so I’ll read it in sections and follow the brief exactly.Continuing through the rest of the packet so the merge scan is based on the full brief.MERGE LIST — ranked, in the format above

[MERGE] `wmw_grok_mcp.py` + `wmw_gemini_mcp.py` + `wmw_cursor_mcp.py` JSON-RPC chassis into one `mcp_stdio.py` (or similar); leave each vendor’s `run_*` + TOOLS + flags as a thin layer
WHY:     They are one MCP stdio server wearing three filenames. Byte-similar today: `_utf8_stdio`, `_UUID_RE`/`_safe_id`, `_req_str`/`_opt_str`/`_opt_bool`, `handle()` (`initialize`/`ping`/`tools/list`/`tools/call`/`-32601`/`-32602`), `main()` line-loop + per-request `except Exception`, `_safe_cwd` (same job, three implementations). Today’s exhibit is in the Gemini file itself: `gemini-reply` cwd gate “The Grok seat has guarded this since 2026-08-23; the fix was never propagated here.” The earlier 50–150 estimate is **right for the chassis, slightly optimistic for the leftover vendor file**: Grok/Gemini land ~160–200 lines (TOOLS descriptions + `run_grok`/`run_gemini` stay); Cursor stays ~450+ because meter/playpen/YOLO/allowance/guard are not transport.
SHAPE:   One module exports `serve(name, version, tools, tool_call)`, `_safe_id`, `_safe_argv`, `_req_str`/`_opt_str`/`_opt_bool`, `_extract_json(raw, mode="first"|"last_result")`, and **one** `_safe_cwd(cwd, always_approve, *, playpen=None)` using the **union of the strongest checks** (Grok/Cursor `commonpath` + secret-segment lowercasing + Cursor’s APPDATA/LOCALAPPDATA ban + playpen allow-list). Each vendor file keeps: `find_*`, `run_*`, TOOLS text, vendor argv (`DENY_RULES` / `--mode plan` / `--yolo`+spill), footer. Gemini’s “ban the entire home subtree” (`real.lower().startswith(home…)`) is **not** OR’d in — that policy forbids ordinary projects under the profile; resolve it to Grok/Cursor’s exact-root-plus-system-containment when combining.
SAVES:   ~300–350 lines of code (3× ~95-line RPC/validator copy + 3× drifted `_safe_cwd`). **0 tokens per summon** — these files are not on the SKILL/SPINE load path. The save is the one the owner already paid for: a guard fixed once, not thrice.
RISK:    `_extract_json` semantics differ (Grok/Gemini = first dict; Cursor = last `type=="result"` because the CLI streams). A single unparameterized extractor will mis-parse Cursor. Unifying cwd onto Gemini’s home-wide ban would refuse legitimate project paths; unifying onto the weaker Gemini secret-check (case-sensitive, no `.cursor`/`.azure`) would reopen the Grok findings.

[MERGE] Gate-0 / right-size / council consent: Part I §2 + Part VI “The dispatch gate” seat-count + THE COUNCIL “Consent gates the convening” / “When NOT to convene” + THE COUNCIL SEAT LAW clause 6
WHY:     One decision — “does this earn N seats, and does the boss have to say go?” — taught four times. Part I §2 already confesses it: “This clause OWNS the consent gate. It is deliberately restated at the dispatch gate and THE COUNCIL.” Seat Law §6 is the same sentence again: “Consent to spend is not consent to convene: Gate-0’s right-size rule and the fleet test bind first.”
SHAPE:   Part I §2 keeps the two questions, right-size default, earn-a-head, and “panel = explicit go.” Part VI dispatch gate becomes two bullets that *apply* it (disjoint builders → fleet test, boss TOLD; N-way panel → Gate-0, boss ASKED). THE COUNCIL keeps the six-step procedure (Brief → lenses → Gather → Synthesize → Cap → boss rules) and one pointer: “convening is Gate-0.” Seat Law §6 disappears into that pointer; Seat Law keeps the allowance protocol only.
SAVES:   ~180–220 tokens per summon (SPINE is on every load). Roughly the consent paragraph (~180 words) + Seat Law §6 (~40 words) + the Part VI restatement trimmed to ~35 words.
RISK:    A conductor who opens THE COUNCIL and no longer sees “opt-in” in that section may auto-convene. Mitigate with one bold pointer line at the top of THE COUNCIL, not a second copy of the rule.

[MERGE] `read-meters.py` + `calibrate-pool.py` + `dispatch-guard.py`’s `yield_report` into one `meters.py` CLI (`read` / `calibrate` / `yield`)
WHY:     Four files were listed; three of them are one concern: “what is in the tank, how big is the tank, what did accepted work cost.” `calibrate-pool.meter()` reimplements `read-meters.read_cursor()` against the same `GetCurrentPeriodUsage` URL and `%APPDATA%\Cursor\auth.json`. `calibrate-pool.find_cli()` duplicates `wmw_cursor_mcp.find_cursor_agent` paths. `read-meters` still names deleted `bench-burn.py` (“`bench-burn.py` reports what a burn cost”) — calibrate *is* the remaining burn. `yield_report` is Meter Law §4 (“cost per ACCEPTED change”) in code, including the same `WMW_CURSOR_LEDGER` path the Cursor seat writes.
SHAPE:   `python meters.py read [--grok|--cursor|--json]` · `python meters.py calibrate [--probe|--calls N]` · `python meters.py yield <repo> [--days N] [--events CSV]`. Shared: Cursor HTTP client, Grok billing client, ledger path. `dispatch-guard.py` keeps **only** `preflight()` (what `wmw_cursor_mcp._guard()` actually calls) plus its CLI.
SAVES:   ~80–110 lines of duplicated HTTP/auth/CLI-lookup, plus one operator entrypoint instead of three. Not a summon-token cut.
RISK:    Calibrate *spends* on purpose; read is documented “Read-only. Nothing here spends.” One binary with a `--calls` subcommand can be invoked carelessly. The subcommand names must make the spend loud (`calibrate`, not `run`). Yield’s vendor-CSV lane and the live HTTP needle are different oracles — keep them as separate subcommands, not one blended number.

[MERGE] `allowance.py` + the Cursor seat’s spend-gate (`_allowance` / `_allowance_window_s` / `_allowance_calls` / `_recent_billable` / COUNCIL_LOCK_*)
WHY:     THE COUNCIL SEAT LAW says the allowance is “a real record the transport checks before it spends.” Today the record (`allowance.status`) only answers “is there an unexpired grant?” Consumption is reimplemented in the Cursor wrapper against `bench-spend.jsonl`, and `allowance.py` is `importlib`-loaded **three times per call**. The comment in `run_cursor` is the smoking gun: enforcement used a hardcoded 10-minute window so `"10/week"` was policed as `"10 per 10 minutes"` until `window_seconds` existed — because the bound and the counter lived in two files.
SHAPE:   `allowance.status(seat, ledger=…)` returns permitted/refused using grant **and** rolling count. Cursor calls it once. Delete Cursor’s triple loader, `_recent_billable`, and the hardcoded `COUNCIL_LOCK_MAX`/`WINDOW` as a second cap — the grant *is* the cap (`calls`/`window`). Env override `WMW_CURSOR_COUNCIL_LOCK=off` stays as a deliberate lift, documented next to `grant`.
SAVES:   ~80–100 lines in `wmw_cursor_mcp.py` plus three disk loads → one. Clearer law-to-code: one bound, one checker.
RISK:    Council-lock and allowance are *almost* the same (rate-limit metered fan-out) but the lock’s copy is a political ruling (“subscription seats only”). If the grant is “25/week” and someone wants a tighter 10-minute panic cap, that second cap needs a named field on the grant, not a silent leftover constant.

[MERGE] Deck `SKILL.md` restatements of SPINE (On-invocation step 2 preflight, NARRATE IN COLOR vendor list, doubled “adds nothing” paragraph)
WHY:     SKILL says “This file does not restate it,” then restates it. Step 2 is Transport Law #4 + Part VI preflight in other words (`claude mcp list`, `UNKNOWN LINEAGE`, the three independence statuses). NARRATE IN COLOR re-lists 🟡➤/🟠/🔵/⚫/🟢 which THE NOTATION already owns. Lines 33–39 say the Deck adds nothing **twice** in consecutive paragraphs.
SHAPE:   Keep DEPENDS, load receipt, FUEL MODE (Deck-only), invariants block (Principle 9 requires the verbatim copy). Step 2 becomes: load `SPINE-WIRING.md`, probe per Part VI + Transport Law #4, print arsenal + independence status. Color section: “Render THE NOTATION plain, by MODEL.” Collapse the doubled delta paragraph to one.
SAVES:   ~250–320 tokens per summon on the SKILL load (step 2 is ~200 words; color list ~50; doubled paragraph ~80).
RISK:    A conductor who never opens SPINE will probe wrong (CLI `--version` as “online”). The receipt already says “loaded, not ready” and step 2 is the ready-gate — keep **one** operational sentence naming MCP-connected vs CLI-fallback, not the full preflight essay.

[MERGE] THE METER LAW + THE COUNCIL SEAT LAW into one SPEND LAW
WHY:     Seat Law §5 (“Unknown cost fails closed… THE METER LAW”) *is* Meter Law §1. Seat Law §6 *is* Gate-0 (covered above). What Seat Law uniquely adds is the allowance protocol (§§1–4: free sits free; spenders need a bound; within it don’t re-ask; past it refuse). Two headings for one spend decision.
SHAPE:   THE METER LAW grows an “Allowance protocol” subsection (Seat §§1–4 + the “Enforced… `allowance.py`” wiring note). Drop the separate COUNCIL SEAT LAW heading. Notation’s meter-mark bullet stays in THE NOTATION (glyphs are not the grant).
SAVES:   ~80–120 tokens per summon (duplicate fail-closed + special-move lines and a second H2).
RISK:    Readers hunting “council seat” by name miss the grant rule. Keep the phrase “council seat” in the subsection title.

[MERGE] Fleet-legality test: Part I §2’s five-name summary + Part IV’s list
WHY:     One test wearing two counts. Part I §2: “all five hold … Declared · Bounded · Accounted · still-Principle-3 · Authority-inheritance.” Part IV: “all five hold” then lists **seven** (adds Destined, Governed where it RUNS). That is not cross-reference, it is fork.
SHAPE:   Part IV owns the list (whatever the real membership is after the Destined/Governed additions). Part I §2 keeps one clause: “A fleet is legal only if Part IV’s test holds.”
SAVES:   ~40–60 tokens per summon, and one fewer way to follow the wrong count.
RISK:    Low if Part IV’s full list is the survivor. High if someone “merges” by deleting Destined/Governed — those are load-bearing (empty write-set still bills; vendor control plane vs shop file).

[MERGE] Part VI “Who may review” + Part IV anti-laundering “two legal paths” (Transport Law #2 keeps the MCP-specific sentence only)
WHY:     Review dispatch: “a different effective-model vendor + lineage … OR a boss-launched fresh seat — never the builder’s own producing lineage.” That is Part IV’s anti-laundering guard copied into the mechanics chapter. Transport Law #2 then says fresh is “necessary, not sufficient” and points at Part IV again.
SHAPE:   Part IV keeps the two paths + declared-seat-lineage + “a name is not a lineage.” Review dispatch starts at **Route by FIT** and the four-item reviewer ticket. Transport Law #2 stays as the one MCP sentence: reviewers are always fresh calls; freshness ≠ independence (pointer).
SAVES:   ~80–100 tokens per summon.
RISK:    Review dispatch is what a conductor reads while writing a reviewer ticket. A pointer-only section may lose the “never the builder’s reasoning” rule if that line is treated as part of the who-may-review blob — it is not; keep it with the four-item ticket.

KEEP APART — what only looks mergeable, and why merging it would hurt

- **The three `run_*` functions and TOOLS description strings.** Same envelope, different CLIs: Grok `--prompt-file` + `DENY_RULES`/`MCPTool`/`--disallowed-tools Agent`; Gemini argv prompt + `MAX_ARGV_PROMPT` + `--mode plan` vs `--dangerously-skip-permissions` + `brain: UNREPORTED`; Cursor playpen spill (PowerShell `.cmd` injection), `--yolo`/`--mode ask`, `spend_credits`, YOLO allowlist, ledger. One `run_vendor` with a flag table will recreate the Gemini-lag class of bug inside a switch statement.
- **Read-only *mechanisms*** (Grok deny-list, Gemini `--mode plan`, Cursor `--mode ask`). Same *intent*, incompatible argv. Shared layer: a boolean `always_approve`; vendor hook supplies flags. Do not share `DENY_RULES`.
- **`SKILL.md` vs `SPINE.md` as files.** Loader vs engine. Concatenating them puts gold-baton/FUEL MODE into every TRM/TRTO inherit. Shrink restatement (merge 5); do not file-merge.
- **`allowance.py` vs `meters.py`.** Local permission record on `~/.anderson-method/allowances.json` vs vendor HTTP/CSV observation. Different failure domains (missing file = fail closed vs network/auth). Combining yields a god-CLI that can grant spend and burn pool in one place.
- **`preflight()` vs meters.** “May this *launch*” (empty repo / no source — the Aug 21–22 shape) vs “what did it *cost*.” Cursor already imports only `preflight`. Folding it into meters means a broken CSV parser can take down the write gate.
- **Principle 6 vs Principle 10.** Both say “the human is the judge.” P6 is authority (only he merges). P10 is anti-dump (a blocked seat re-plans; he is usually on a phone). Merging them loses the “don’t make him the transport” rule.
- **Principle 8’s review-round cap vs Part VI Escalation.** Both say “two then stop.” Escalation is *ticket/capability failure* (fix ticket → retry → one seat up → boss). P8/Part VII/autonomous-hours is *dispute* (two rounds then ruling queue). Merging produces “retry the cheaper seat” on a design fork or “escalate to the boss” on a typo.
- **Cursor meter/playpen/YOLO/council policy vs Grok/Gemini chassis.** “The one metered seat” is why Cursor is 762 lines. Pulling YOLO allowlist or `SPEND_LEDGER` into shared stdio either meters Grok (wrong) or makes the shared module Cursor-shaped (the other two stop compiling mentally).
- **Worker statuses (`DONE` / `NEEDS_CONTEXT` / …) vs finding ranks (`BLOCKER` / `MATERIAL` / …).** SPINE already: “One axis per line, never mixed.” Merging them into one enum makes a `BLOCKED` worker look like a `BLOCKER` finding.
- **Part I Diagnose/Design fork vs THE COUNCIL procedure.** Fork *classifies*; council *runs the panel*. The fork already points. Inlining the six steps into the fork duplicates merge 2 the other way.
- **Ladder of Truth vs Part V three-list containment.** Evidence rank vs “did the reviewer see the actual delta.” Merging them turns a missing file in the manifest into a NOT PROVEN vibe.
- **`armcheck.py` vs production modules.** Canaries that “ATTACKS the seats with live calls.” Inlining them into wrappers or `meters.py` either spends on every import or never runs the attack path (the suite’s own past failure mode).
- **THE NOTATION vs `BLOODLINE_MARK` / `METER_MARK` in Cursor.** Law vs the seat that must print it. Putting the dict into SPINE loads shop glyphs on every summon; putting THE NOTATION into Python means a code bump to change a doctrine mark.

THE ONE MERGE worth doing if only one happens

The shared MCP chassis (`wmw_grok_mcp.py` / `wmw_gemini_mcp.py` / `wmw_cursor_mcp.py` → one stdio helper + three vendor layers). Two doctrine-delete rounds already cut the easy summon tokens; this is the combine those rounds could not do. It is the orchestrator’s actual machine — where/how a seat is summoned, work placed in front of it, output brought back — and it is the duplication that left Gemini’s write-escalation unguarded after Grok was fixed. If the metric is summon tokens instead of code, do Gate-0 (merge 2) + SKILL pointers (merge 5) first; they are the only items here that move the ~13,797 number.

CONFIDENCE

0.8 on the wrapper chassis and the Gate-0 quadruple (anchors are explicit, including SPINE’s own “deliberately restated”). 0.7 on meters-CLI and allowance-as-the-one-counter (right shape; calibrate-vs-read spend/read-only mix is the failure mode to design for). 0.6 on Meter Law ⊕ Seat Law as a heading merge (real overlap at §§5–6; §§1–4 are still a distinct protocol). The 50–150 leftover-vendor estimate is **not** right for Cursor and is **tight** for Grok/Gemini unless TOOLS prose also moves.