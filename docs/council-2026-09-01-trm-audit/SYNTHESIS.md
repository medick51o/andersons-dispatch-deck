# SYNTHESIS — TRM/TRTO audit council · 2026-09-01 · conductor: Claude Fable 5.1 (🟡➤)

Seats: 🟢 Gemini 3.1 Pro (cost/efficiency) · ⚫ Grok 4.6 (engagement/show) · 🟣➤🔵 gpt-5.3-codex via Cursor
(structure/topology). Blind, one lens each, read-only. Signed reads: GEMINI.md, GROK.md, CURSOR-GPT.md.

## Unanimous (3/3)
- Delete `cat-in-charge` as a live skill; archive it. Grok: "a poison door" (old law 8 + screenshot mandate
  that SHOW repealed). Cursor: BLOCKER. Gemini: BLOCKER.
- Strip the stale ALIGNMENT / PERSISTENT SEATS footers from every launcher; the receipt interpolates versions.
- Rewrite CREW's "No meter wrap in this shop" to obey SPINE's SPEND LAW; name `wmw-cursor`.
- The "three-question interview" is a ghost (SPINE L306 points at nothing; trm step 2 runs it). All three: fix.
- SHOW vibe laws 4–5 restate the house rules. Trim (Grok/Cursor) or cut (Gemini).
- One owner for the fuel rule; tiers reference, never restate.
- Publishing needs a script with a hash/version parity check.

## Split, and the rulings
1. **Where CREW lives.** Gemini: hub owns SPINE+CREW+SHOW. Cursor: deck owns SPINE, trto repo owns CREW+SHOW,
   hub is a mirror. Grok: deck→SPINE, hub→CREW + trm SKILL, trto→SHOW + trto SKILL.
   **RULING: Grok's table.** SPINE + wiring are the deck's (it owns the seats). CREW is the TRM brand's
   crew and `/trm` lives in the hub, so the hub owns CREW. SHOW and the TRTO launcher are the branch's.
   Everything else is a publish-copy refreshed by `publish.ps1` (in the deck) with a `-Check` parity mode.
2. **The ghost interview.** Gemini: move the interview into the trm launcher. Grok: kill it, load
   PLAN-CARD.md if present, else confirm the CAST-MAP. Cursor: re-anchor or restore.
   **RULING: Grok.** An earlier council deleted the interview from SPINE on purpose; the cast-map already
   records vendors. Restoring it would rebuild a question wall. SPINE L306 rewritten; trm step 2 rewritten.
3. **The Rival Clause.** Gemini: drop it from SHOW, CREW owns fuel. Grok: one rule, three costumes; deleting
   the Rival kills the bit. Cursor: CREW canonical, SHOW dramatizes only.
   **RULING: Grok + Cursor.** CREW §2 Corner-Man owns the RULE (portable: downloaders lack the house
   rules). SHOW keeps the Rival as a costume that already cites CREW §2. The deck keeps its 6-line
   fuel switch. The fourth rendering (cat-in-charge) dies with the skill.
4. **goteamrocket.** Gemini/Cursor: delete or reduce. Grok: keep the megaphone, strip its footer.
   **RULING: keep.** It is already the thin alias Cursor asked for, it is 1.4KB, and it is the battle cry.
5. **How much of CREW/SHOW to cut.** Gemini: strip tutorials, keep personas. Grok: keep the sitcom bible,
   unload CREW's provenance + install paragraph (the real per-summon win). Cursor: trim overlap prose.
   **RULING: Grok's cut, Gemini's principle.** CREW §7 provenance moves to PROVENANCE.md (not loaded);
   the persistent-seats paragraph shrinks to a pointer at the wiring; SHOW's bible stays; vibe 4–5 become
   one-liners that present the house rules rather than reprint them.

## Grok's lens catch, adopted
TRTO went dark (last summon 2026-08-11) while the deck stayed active: a 25k-token summon lost to a 13k one.
The ADHD tax is the ceremony between invoke and "what's tonight's episode, boss?" So the cut lands on CREW's
load, not SHOW's bible. Jessie-never-gated and the 65/15 talk mix stay untouched.

## Disagreement carried to the boss (not resolved here)
None blocking. Gemini's "trust the latent space, strip step-by-step tutorials from SPINE" is a SPINE-wide
trim outside tonight's fence; queued as the next council brief ("what in SPINE does Fable 5.1 do unprompted").
